"use client";
import { useState } from "react";
export function RequestChanges({ siteSlug, entryId }: { siteSlug: string; entryId: string }) { const [message, setMessage] = useState(""); async function submit(formData: FormData) { const response = await fetch(`/api/cms/entries/${entryId}/request-changes`, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ siteSlug, note: formData.get("note") }) }); const result = await response.json(); setMessage(result.message ?? result.error); } return <form action={submit} className="mt-3 flex flex-wrap gap-2"><input name="note" required maxLength={5000} placeholder="Required review note" aria-label="Review note" className="min-w-64 flex-1 rounded-lg border border-black/15 px-3 py-2 text-sm"/><button className="rounded-lg border border-black/15 px-3 py-2 text-xs font-semibold">Request Changes</button><p aria-live="polite" className="w-full text-xs text-black/50">{message}</p></form>; }
